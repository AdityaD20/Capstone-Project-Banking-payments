package com.aurionpro.app.entity.enums;

/**
 * Defines the types of user roles in the system.
 * The "ROLE_" prefix is a standard convention for Spring Security.
 */
public enum RoleType {
    ROLE_BANK_ADMIN,
    ROLE_ORGANIZATION,
    ROLE_EMPLOYEE,
    ROLE_VENDOR
}